auto-install-peers=true
strict-peer-dependencies=false

# `npm version` creates monorepo-scoped tags (e.g. blocks-engine-v0.1.0)
# to match the per-package tag convention used by php-transformer/figma-transformer.
tag-version-prefix=blocks-engine-v
